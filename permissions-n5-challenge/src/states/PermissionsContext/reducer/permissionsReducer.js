import { PERMISSIONS_SET_PERMISSIONS, PERMISSIONS_SET_PERMISSIONS_TYPE } from '../actions/permissionsActions';

const initialState = {

};

const reducer = (state, action) => {
    const { type, payload } = action;
    switch (type) {

    }
};

export{ initialState, reducer }